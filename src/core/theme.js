import { DefaultTheme } from "react-native-paper";

export const theme = {
    ...DefaultTheme,
    color: {
        ...DefaultTheme.colors,
        text: '#00000',
        primary: '#560CCE',
        secondary: '#414757',
        error: '#f13a59',
    }
}